# LLM_LUT/v6 build_lut_ffn_output.py 依赖
# Python 3.10 + CUDA 12.4 driver (driver version 12040)
#
# 服务器上建议用这个命令安装（会自动使用 cu124 wheel）：
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu124
#
# 如果已经装了 CUDA 12.1 或更高版本且驱动兼容，也可以去掉 --index-url 用默认的 torch。

torch==2.5.1+cu124
numpy<2
tqdm
