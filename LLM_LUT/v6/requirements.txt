# LLM_LUT/v6 build_lut_ffn_output.py / run_model_eval.py 依赖
# Python 3.10 + NVIDIA driver (driver version 12040 已验证可用)
#
# 注意：torch 的 +cu124 版本只在 PyTorch 官方 wheel 站上有，aliyun/pypi 主站没有。
# 推荐安装命令（把你的 PyPI 镜像作为 extra，torch 从 PyTorch 官方站下载）：
#
#   pip install -r requirements.txt \
#     --index-url https://download.pytorch.org/whl/cu124 \
#     --extra-index-url https://mirrors.aliyun.com/pypi/simple/
#
# 如果你的服务器能直连 PyTorch 官方站，也可以：
#
#   pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cu124

torch==2.5.1+cu124
numpy<2
tqdm
transformers>=4.40.0
datasets>=2.0.0
accelerate>=0.20.0  # needed by transformers, but never use device_map="auto" in our scripts
protobuf>=3.20
sentencepiece
