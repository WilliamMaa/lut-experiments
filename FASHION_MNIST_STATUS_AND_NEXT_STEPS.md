# Fashion-MNIST 实验全记录与下一步方向

> 日期：2026-05-08  
> 核心目标：存算一体设备上的 O(1) LUT 查表动态参数注入

---

## 一、实验全景

### 1.1 基线与数据增强

| 方案 | 维度 | 参数量 | 训练 | 测试 | 差距 |
|------|------|--------|------|------|------|
| 静态基线 | 16 | 13K | — | **87.22%** | — |
| 静态基线 | 24 | 19K | — | **87.85%** | — |
| 静态基线 | 32 | 26K | — | **87.62%** | — |
| 静态基线 | 48 | 39K | — | **88.42%** | — |
| 静态 + Label Smoothing | 24 | 19K | 92.82% | **87.57%** | 5.25% |
| CNN Teacher 蒸馏 → FC Student | 24 | 19K | — | **87.75%** | — |

**关键发现：**
- 扩大 backbone（16→48）仅提升 **+1.2%**，性价比极低
- Label Smoothing 对 24-dim 无显著提升（87.57% vs 87.85%）
- **蒸馏完全失效**：Teacher CNN 92.19% → Student FC 87.75%，增益仅 +0.13%
- 原因：FC 缺乏局部感知能力，无法模仿 CNN 软标签中的空间结构信息

### 1.2 LUT 动态注入

| 方案 | 注入位置 | 动态机制 | 基线 | 测试 | 增益 |
|------|----------|----------|------|------|------|
| LUT-3 原版 | 输出层 | 20-dim 乘性 | 87.02% | **87.72%** | +0.70% |
| LUT-3 v2 | 输出层 | 40-dim 乘加+大表+加权 | 87.03% | **87.83%** | +0.80% |
| **LUT-3 main** | **backbone_main** | **340-dim 替换 weight+bias** | 86.58% | **88.12%** | **+1.54%** |
| VQ-LUT | 输出层 | 64 码本离散选择 | 86.97% | **86.43%** | **-0.54%** |
| MLP 端到端（参考） | backbone_main | 340-dim 生成 | 87.02% | **87.82%** | +0.80% |

**关键发现：**
- **注入位置比查表策略更重要**：输出层乘加（+0.8%）< backbone_main 替换（+1.54%）
- LUT 查表和 MLP 生成效果接近（88.12% vs 87.82%），验证了 O(1) 路线的可行性
- VQ 离散化在复杂数据上**完全失效**，per-cluster 粒度太粗
- 所有 LUT 方案训练-测试差距 3-5%，仍有一定过拟合

### 1.3 当前天花板

| 方法 | 最高测试集 |
|------|-----------|
| 纯静态（任何维度） | ~88.4% |
| 蒸馏 | ~87.8% |
| LUT 动态注入 | **88.12%** |

> **结论：简单全连接网络在 Fashion-MNIST 上的绝对天花板约为 88.5%，动态注入无法突破这个上限。**

---

## 二、核心教训

1. **动态注入是"锦上添花"，不是"雪中送炭"**
   - backbone 的表达能力决定了天花板，动态参数只能在天花板之下分配资源
   - 16-dim bottleneck 能编码的信息太少，LUT 再强也救不了

2. **注入位置比注入形式更重要**
   - 替换 backbone_main（340-dim）比输出层乘加（20-dim）增益翻倍
   - 越靠近特征层，动态参数的 leverage 越大

3. **蒸馏对 CNN→FC 天然失效**
   - Teacher 的软标签编码空间结构信息，FC Student 无法模仿
   - 架构鸿沟比性能差距更难跨越

4. **VQ 离散化在类内方差大的数据上不可行**
   - 64 个码本无法覆盖 Fashion-MNIST 的复杂类内变化
   - per-cluster 粒度太粗，丢失关键个性化信息

5. **query_proj 是存储大头**
   - LUT-3 main 中 query_proj（133K）比 LUT 表（114K）还大
   - 压缩 query_proj 是降低总存储的关键

---

## 三、论文推荐

基于当前瓶颈，推荐阅读以下论文，按优先级排序：

### 优先级 1：低秩动态参数（解决 query_proj 压缩）

**LoRA: Low-Rank Adaptation of Large Language Models** (Hu et al., ICLR 2022)
- 核心思想：用低秩矩阵分解来近似大权重变化，参数量从 d×d 降到 d×r + r×d（r << d）
- 对我们的启发：query_proj 输出 1020-dim 坐标，如果坐标之间存在低秩结构，可以用低秩分解大幅压缩

**KronA: Parameter Efficient Tuning with Kronecker Adapter** (Edalati et al., 2022)
- 用 Kronecker 积做更紧凑的参数分解

### 优先级 2：多分辨率哈希编码（优化 LUT 查表机制）

**Instant Neural Graphics Primitives** (Müller et al., SIGGRAPH 2022)
- NGP 的多分辨率哈希编码和我们的 LUT-3 高度相似
- 可以学习：哈希冲突处理、多尺度特征融合、训练稳定性技巧
- 关键问题：NGP 的表存储的是特征向量，后面接 MLP 解码；我们的 LUT 直接输出参数残差——这两者差异很大

### 优先级 3：条件计算与硬件协同设计

**Dynamic Filter Networks** (De Brabandere et al., NeurIPS 2016)
- 根据输入动态生成卷积核，和我们的工作思路最接近
- 可以看他们如何处理"动态参数生成"的计算效率问题

**Reversible Column Networks** (RevCol, ICCV 2023)
- 虽然不是直接相关，但其中的多尺度特征解耦思想可能对注入位置选择有启发

### 优先级 4：蒸馏的结构性局限（验证我们的发现）

**Born Again Neural Networks** (Furlanello et al., ICML 2018)
- 讨论为什么蒸馏对同架构效果最好，跨架构效果差

---

## 四、下一步方向

### 方向 A：压缩 query_proj（短期，低成本）

当前 query_proj：16→128→1020，133K 参数。

**方案 A1：减少尺度**
- 3 尺度 → 2 尺度，query_proj 输出 680-dim
- 16→64→680，~45K 参数

**方案 A2：共享坐标**
- 340 维分成 34 组，每组 10 维共享坐标
- query_proj 输出 68-dim
- 16→32→68，~2.8K 参数

**方案 A3：低秩分解**
- query_proj 输出视为一个低秩矩阵
- 16→64→(r=8)→1020，参数量大幅降低

### 方向 B：换一个 backbone 架构（中期，不改变"微型网络"本质）

当前困境：纯 FC 对 Fashion-MNIST 的天花板锁死在 88.5%。

**方案 B1：1D-CNN**
- 1-2 层 1D 卷积（把 28×28 展平为 784-dim 序列）
- 参数量仍然很小（<30K），但有局部感知能力
- LUT 仍然注入最后几层

**方案 B2：轻量 Transformer / MLP-Mixer**
- 但计算量可能太大，不适合 CIM

### 方向 C：换一个数据集验证（短期，低成本）

Fashion-MNIST 可能太极端。验证 LUT 在"中等复杂度"数据上的表现：

- **Kuzushiji-MNIST**：日文手写，类内变化适中
- **EMNIST Letters**：字母，结构比 Fashion-MNIST 规整
- **CIFAR-10 灰度化**：更复杂，但如果 LUT 有效，说服力更强

### 方向 D：混合粒度注入（探索性）

吸取 VQ 失败的教训，但不用完全离散化：

- **类别级别基础参数**：10 个类别各维护一组 340-dim 基础动态参数
- **LUT 残差修正**：在类别基础参数上，LUT 输出一个小残差
- `final_param = class_base_param[class_id] + lut_residual(query)`
- 这样既有稳定性（类别基础），又有个性化（LUT 残差）

---

## 五、个人建议

**如果只能选一个方向推进：**

> **先做方向 C（换数据集）**，快速验证 LUT 方法本身是否有效。
>
> 如果 LUT 在 Kuzushiji-MNIST 上能带来 **2~3% 增益**，说明 Fashion-MNIST 的结果只是"数据太极端"，方法本身有价值。
>
> 如果 LUT 在所有中等复杂度数据上都只有 **<1% 增益**，说明需要回到方向 B（换 backbone 架构）或方向 A（压缩 query_proj 优化存储效率）。

**如果愿意投入时间：**

> 同时做方向 A（压缩 query_proj）和方向 C（换数据集）。前者解决工程问题，后者验证方法论。
