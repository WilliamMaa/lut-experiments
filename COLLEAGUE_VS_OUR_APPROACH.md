# 同事方案 vs 我们的方案对比分析

> 文件：`lut_train_1000.py`（同事的"终极量产版"）
> 日期：2026-05-16

---

## 一、同事方案核心架构

### 1.1 设计思路

```
输入特征 x (B, 384, H, W) + 基线 base + 索引 idx
    ↓
查表 A: Embedding(65536, 384×32) → wA (B, H, W, 384, 32)
查表 B: Embedding(65536, 32×128) → wB (B, H, W, 32, 128)
    ↓
LoRA 分解：x → wA → wB → lora_out (B, 128, H, W)
    ↓
输出 = base + alpha × lora_out
```

### 1.2 关键设计

| 特性 | 同事方案 | 数值 |
|------|---------|------|
| **查表机制** | 两张大表 (A + B)，LoRA 分解 | `65536 × (384×32 + 32×128)` |
| **参数量** | 两张 Embedding 表 | **约 10 亿参数**（4GB+） |
| **输入维度** | 384 维通道 + 空间 (H,W) | 高维特征图 |
| **索引来源** | 预计算的离散索引 (65536 个 token) | 离线数据集中提供 |
| **训练方式** | 离线蒸馏 + 梯度累加 + 混合精度 | 分布式 DDP，长程训练 (500-1000 epochs) |
| **损失函数** | 方向 (余弦) + 幅度 (SmoothL1) 复合 | 3:1 加权 |
| **Rank** | 32 | LoRA 低秩分解的秩 |

---

## 二、我们的方案核心架构

### 2.1 设计思路

```
输入图像 1×28×28
    ↓ CNN conv → 1568-dim flat
    ├─ flat[:class_dim] → Linear → feat_class → 分类头
    └─ flat[:lut_dim] → BN + Sigmoid → feat_lut_coords (0~1)
                                    ↓
                    LUT-FiLM (query_dim × 40 × [32,8,2])
                                    ↓
                    gamma, beta (各 20-dim)
                                    ↓
输出 = backbone_main(feat_class) × (1+gamma) + beta → classifier
```

### 2.2 关键设计

| 特性 | 我们的方案 | 数值 |
|------|-----------|------|
| **查表机制** | 多尺度 1D 插值表 | `query_dim × 40 × (32+8+2)` |
| **参数量** | 三张多尺度表 + base | **6.7K~27K 参数**（27KB~108KB） |
| **输入维度** | 16/8/4-dim 低维特征 | Fashion-MNIST 整图 |
| **坐标来源** | **CNN conv 输出直接切片 + BN + Sigmoid** | 零参数，推理零计算 |
| **训练方式** | 端到端训练 | AdamW，100 epochs |
| **损失函数** | 交叉熵 + (可选) uniformity loss | 分类驱动 |
| **Rank** | N/A | 直接输出 40-dim FiLM 参数 |

---

## 三、核心差异对比

| 维度 | 同事方案 | 我们的方案 | 差异本质 |
|------|---------|-----------|---------|
| **应用场景** | YOLO 目标检测（高维特征图 384×H×W） | 图像分类（整图 28×28） | 检测需要空间级动态，分类只需样本级动态 |
| **查表对象** | **空间像素级**（每个 token 独立查表） | **样本级**（每张图一个坐标向量） | 同事的表是"位置相关"，我们是"样本相关" |
| **参数规模** | 10 亿（4GB+） | 6.7K~27K（27KB~108KB） | 相差 **4~6 个数量级** |
| **索引来源** | 预计算离散索引 (65536 个) | CNN 实时输出连续坐标 | 同事依赖离线数据，我们是端到端 |
| **推理计算** | 查表 + 两次矩阵乘法 (LoRA) | 查表 + 插值 + FiLM 调制 | 同事有 LoRA 计算，我们是纯加法/乘法 |
| **训练范式** | **离线蒸馏**（预提取特征，固定索引） | **端到端**（CNN + LUT 联合优化） | 同事是两阶段，我们是一阶段 |
| **硬件目标** | GPU 训练 + 推理 | **存算一体 SRAM 查表** | 同事追求准确率，我们追求功耗/延迟 |
| **过拟合控制** | 梯度累加 + 长程训练 + 复合 loss | weight_decay + 梯度裁剪 + 表压缩 | 手段不同，目标一致 |
| **表初始化** | std=0.1（激进） | std=0.02（保守） | 同事怕早期僵局，我们怕初期爆炸 |

---

## 四、本质差异总结

### 4.1 同事方案的本质

**"用大表存储知识，按需提取"**

- 两张 65536 行的大表存储了"所有可能 token 的权重知识"
- 通过 LoRA 分解降低单表维度，但总参数仍然巨大
- **核心假设**：表足够大，就能覆盖所有场景的权重需求
- **适合场景**：GPU 推理、离线蒸馏、目标检测（空间级动态）

### 4.2 我们方案的本质

**"用极小表做动态调制，从低维特征恢复能力"**

- 表只有 6.7K~27K 参数，完全放在 SRAM 上
- 通过 FiLM 调制实现动态仿射变换
- **核心假设**：表不需要大，只要能补回低维特征丢失的信息就够了
- **适合场景**：存算一体芯片、边缘设备、图像分类（样本级动态）

### 4.3 谁对谁错？

**都对，但解决的问题不同**：

| 指标 | 同事方案 | 我们的方案 |
|------|---------|-----------|
| 准确率提升 | 可能更高（10 亿参数硬背） | 适中（6.20% 增益已不错） |
| 存储开销 | 4GB+（需要 DRAM） | 27KB（SRAM 足够） |
| 推理延迟 | 高（LoRA 矩阵乘法） | 极低（O(1) 查表 + FiLM） |
| 功耗 | 高（GPU 级计算） | 极低（SRAM 读取 + 简单运算） |
| 硬件适配 | GPU/TPU | **存算一体 CIM** |

---

## 五、我们可以从同事方案学到什么？

### 5.1 LoRA 分解思想

同事用 `table_A (c_in × r)` + `table_B (r × c_out)` 分解大矩阵。

**我们可以尝试**：
- 把 40 维 FiLM 输出分解为 `r=8` 的低秩形式
- 表存储从 `query_dim × 40 × size` 降到 `query_dim × (16×8 + 8×40) × size`
- **代价**：增加一次中间矩阵乘法，可能违反 O(1) 原则

### 5.2 Alpha 缩放机制

同事的 `self.alpha = nn.Parameter(torch.ones(1, c_out, 1, 1))` 让模型自适应调整 LoRA 输出的幅度。

**我们已经在用**：
- `base_gamma` 和 `base_beta` 就是类似的机制
- 但我们是逐样本动态的，同事是全局静态的

### 5.3 复合损失函数

同事用 `3×cosine_similarity + smooth_l1` 同时优化方向和幅度。

**我们可以尝试**：
- 在 uniformity loss 之外，加方向约束（让动态残差和基线输出方向一致）
- 但分类任务的交叉熵已经隐含了方向约束，可能不需要

### 5.4 激进初始化

同事用 `std=0.1` 打破早期僵局。

**我们可以尝试**：
- 把表初始化从 `std=0.02` 调到 `std=0.05` 或 `0.1`
- 可能加速收敛，但需要配合梯度裁剪

---

## 六、一句话总结

> **同事的方案是"大表硬背"（10 亿参数 + 离线蒸馏 + LoRA 分解），适合 GPU 上追求极致准确率；我们的方案是"小表巧补"（6.7K~27K 参数 + 端到端 + FiLM 调制），专为存算一体设备的极低功耗设计。两者解决的问题不同，但同事的 LoRA 分解和 Alpha 缩放思想值得我们借鉴。**
